# Backends: storage = local|s3, audit = jsonl|dynamodb
CIS_STORAGE_BACKEND=local
CIS_AUDIT_BACKEND=jsonl

# AWS settings (only used when backends are s3 / dynamodb)
CIS_AWS_REGION=us-east-1
CIS_S3_BUCKET=
CIS_DYNAMODB_TABLE=compliance-audit

# Screening rules
CIS_REVIEW_THRESHOLD=3000000
